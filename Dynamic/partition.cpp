#include<iostream>
#include<stdio.h>
#include<vector>

using namespace std;

get()

int main()
{
    int a,b,c,s;
    cin>>a;
    s=0;
    vector<int > v1;
    for(b=0;b<a;b++)
    {
        cin>>c;
        v1.push_back(c);
        s+=c;
    }
    if(s%2==1)
    return;
    cout<<get(v,s/2);

}
